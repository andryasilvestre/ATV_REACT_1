import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Button } from 'react-native';
import React from "react"

const about = () => {
    <view>
        <img src={require('./assets/img/maggie.png')} />
    </view>
}